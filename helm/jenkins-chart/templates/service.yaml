apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ include "jenkins.name" . }}
